CLAUDE_REVIEW_RESULT_V1

# Plan Review R1 — Remove Legacy Pages Output (Phase D cleanup)

- Reviewer: Claude Code (external reviewer, codex-primary mode; reviewer only)
- Spec: `docs/superpowers/specs/2026-07-13-single-repo-hugo-pages-migration-design.md`
- Plan: `docs/superpowers/plans/2026-07-13-remove-legacy-pages-output.md`
- Base: `origin/master` @ `c44082e` (Phase B merged, Phase C deployed). Reviewed in worktree `feat/cleanup-legacy-pages`.
- Scope: (A) alignment with spec, (B) execution completeness. Not spec-direction, not Markdown style.
- Date: 2026-07-13

## Convention context

No `AGENTS.md` at the project root — convention context is **unavailable**. Recorded per instruction.

## Verifications performed

- **Deletion targets are all tracked and in scope (B1):** the 5 root files (`404.html`, `index.html`, `index.xml`, `robots.txt`, `sitemap.xml`), the five generated dirs (`assets/`,`categories/`,`page/`,`post/`,`tags/` — 49 tracked files), and **every** tracked `._*` path resolve either to repo root (7 files: `._.DS_Store`, `._assets`, `._categories`, `._page`, `._post`, `._robots.txt`, `._tags`) or under a forbidden prefix (`assets/._*`, `categories/._*`, `page/._*`, `post/._*`, `tags/._*`). No tracked `._*` exists under `docs/`, `site/`, or `scripts/`. So `git rm -r -- <files> assets categories page post tags ._*` (Task 1 Step 3) removes all of them and the new verifier goes GREEN.
- **`git rm ._*` will succeed (B1):** the 7 root `._*` are tracked (committed to the original public repo before `.gitignore` was introduced), so `git rm` removes them despite the `._*` ignore rule; the shell glob expands to those 7 tracked files.
- **Modify-file accuracy (B3):** the current `.github/workflows/hugo.yml` is exactly the startup-state workflow the plan targets — `on: pull_request(master) + workflow_dispatch` with **no** `push`, exactly **two** `github.event_name == 'workflow_dispatch'` guards (lines 48 upload, 54 deploy), and four full-SHA-pinned official Actions. Task 2 Step 1's pre-edit assertions and Step 2's "change both conditions" therefore hold; Step 3's post-edit counts (`push`=1, `!= 'pull_request'`=2, `== 'workflow_dispatch'`=0, actions=4/pinned=4) are internally consistent.
- **Verifier REQUIRED_PATHS (B1):** `site/themes/PaperMod` is a tracked gitlink at `160000 efe4cb45…`, and `git ls-files` lists it, so `verify-repository-state.py`'s required-path check passes in CI and locally.

## Half A — Alignment: clean

- **A1 Coverage:** Every Phase D spec item maps to a task — delete the exact root-output allowlist (Spec §9 Phase D → Task 1), preserve `.github/`,`.gitignore`,`.gitmodules`,`docs/`,`site/` (Spec §9/§13 → Task 1 preserve list + verifier `REQUIRED_PATHS`), flip the workflow startup→stable in the **same** PR (Spec §9 Phase D / §7.1 → Task 2), and open the cleanup PR (→ Task 3). Rollback (Spec §12 "回滚清理 PR … 不得强制推送 master") is carried in Post-Merge Handoff step 6.
- **A2 Faithfulness:** The deletion allowlist matches Spec §9 Phase D item-for-item; the stable-state triggers implement Spec §7.1 exactly — `pull_request` builds/verifies only, `push`-to-`master` and `workflow_dispatch` build+deploy (`if: github.event_name != 'pull_request'` on both the upload step and the deploy job). No silent substitution.
- **A3 Scope drift:** none directional. (Sub-threshold note below.)

## Half B — Execution: clean

- **B1 Logic:** verifier RED-before-delete → GREEN-after-delete sequencing is correct; `git rm` stages deletions so `git ls-files` in Step 4 no longer lists them; all deletion targets are tracked so no `git rm` "pathspec did not match" error.
- **B2 Omissions:** rollback path present (revert cleanup PR to restore root artifacts), post-merge observability present (watch Build+Deploy, re-run 73-path HTTP matrix + old-host scan), whitespace check correctly excludes `site/content/**` and `site/archetypes/**` per the byte-unchanged constraint.
- **B4 Sequencing:** `verify-repository-state.py` is created in Task 1 before Task 2 wires it into the workflow; deletion (Task 1) precedes the local build/verify (Task 2 Step 4); rebase-onto-`origin/master` (Task 3 Step 1) precedes push. Each precondition is produced by an earlier task.

## Sub-threshold observation (confidence < 70; not blocking)

**[ALIGN-A3] New `verify-repository-state.py` guard wired into every build (~35).** The spec's control for "生成物重新进入 Git" (§13) is `.gitignore`, which does not cover the root-level output (`404.html`, `assets/`, …). The plan adds a git-index verifier and runs it on every build. This is *extra* machinery the spec did not explicitly request, but it enforces the spec's own cleanup boundary and preserve-list rather than diverging from the direction, and it does not break normal future content PRs (no forbidden paths → passes). Not a directional divergence; recorded only for transparency.

## Pre-mortem (mandatory)

Assuming faithful execution, the single most likely failure is **at the boundary where the root fallback is deleted but the push-triggered deploy must become the sole publisher** — traceable to Spec §7.1 (stable state: `push` to `master` deploys) and §9 Phase D. Concretely: the cleanup PR merges, the `push` run's Build+Deploy must succeed to republish from the Actions artifact, but if that deploy fails transiently, the live site is frozen on the last good deploy while the root-branch fallback is already gone. This is low-probability (Pages is already `build_type: workflow` and the identical build+deploy already succeeded in Phase C run `29222816749`) and is explicitly mitigated by the plan's Post-Merge Handoff (require both jobs green; else rerun last-good, else revert the cleanup PR to restore root artifacts; never force-push `master`), so it is a covered risk rather than a dropped goal — which is why no in-scope step reaches the reporting threshold.

## Verdict

**LGTM** — Alignment: the plan faithfully implements Spec §9 Phase D and §7.1 stable state, deleting exactly the root-output allowlist and flipping the workflow to `push`-deploys/PR-build-only in a single cleanup PR while preserving the required source/workflow paths. Execution: every deletion target and the modified workflow were verified against the live repo, sequencing produces each precondition before use, and the only residual risk (the post-merge push deploy) sits outside this lifecycle and is already covered by the plan's rollback handoff.

CLAUDE_REVIEW_DONE
